import { Engine } from "./Engine";
import { Model } from "./Model";

export class Vehicle {
  idVehicle!: number;
  modelId?: number;
  nameModel?: string;
  brandId?: number;      
  nameBrand?: string;    
  engineId?: number;
  nameEngine?: string;
  year?: number;
}